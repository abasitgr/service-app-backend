const {RegularExp} = require("./regularExpression");
const {PenaltyCharges} = require("./penaltyCharges");
const {BaseCharges} = require("./baseCharges");


module.exports = {
    RegularExp,
    PenaltyCharges,
    BaseCharges
}